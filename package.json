{
  "name": "astero",
  "scripts": {
    "build": "esbuild --bundle src/main.js --outfile=docs/main.js --minify",
    "dev": "esbuild --bundle src/main.js --define:DEBUG=true --outfile=docs/main.js --servedir=docs"
  },
  "dependencies": {
    "kaboom": "3000.0.15"
  },
  "devDependencies": {
    "esbuild": "^0.18.17"
  }
}
